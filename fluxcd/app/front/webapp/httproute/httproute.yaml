apiVersion: gateway.networking.k8s.io/v1beta1
kind: HTTPRoute
metadata:
  name: webapp-route
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: webapp-gateway
      sectionName: http
  rules:
    - backendRefs:
        - name: webapp
          port: 80