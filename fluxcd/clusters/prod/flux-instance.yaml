apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux-instance
  namespace: flux-system
spec:
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./fluxcd/infra/flux-instance/prod
  dependsOn:
    - name: flux-operator
  # kustomize:  
  #   patches:
  #     - target:
  #         kind: Deployment
  #       patch: |
  #         apiVersion: apps/v1
  #         kind: Deployment
  #         spec:
  #           template:
  #             spec:
  #               affinity:
  #                 nodeAffinity:
  #                   requiredDuringSchedulingIgnoredDuringExecution:
  #                     nodeSelectorTerms:
  #                       - matchExpressions:
  #                           - key: role.core
  #                             operator: Exists
  #               tolerations:
  #                 - key: CriticalAddonsOnly
  #                   operator: Exists
  #                   effect: NoSchedule
  prune: true
  interval: 10m
  timeout: 5m